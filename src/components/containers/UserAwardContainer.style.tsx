import styled from 'styled-components';

export const UserAwardContainer = styled.span`
    color: ${(props) => props.color};
    display: inline-flex;
    margin-right: 0.5rem;
    font-size: 1.2rem;
`;
